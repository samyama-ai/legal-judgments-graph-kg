# Benchmarks

Benchmark queries for the Legal Judgments KG (mirrors the Samyama benchmark book format).

Add one `.cypher` (or `.md`) per query with the expected top-N result, e.g.:
- most-cited precedents
- statutes invoked most often
- judges by reversal rate
- citation-network centrality
